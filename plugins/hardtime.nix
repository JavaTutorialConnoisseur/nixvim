_:

{
  opts = {
    enable = true;
    settings = {
      max_time = 1500;
      max_count = 3;

      # I don't use it anyway, and when I do it's for terminal scroll which I want enabled
      disable_mouse = false;
      hint = false;
    };
  };
}
